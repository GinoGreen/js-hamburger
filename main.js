const showMenu = document.querySelector('div.hambuger-menu');
const toggleMenu = document.querySelectorAll('.header-right a i.fa-bars');

toggleMenu.addEventListener('click', function () {
   console.log('hai cliccato sull\'hamburger');
});